[user]
	name = Leo Bergnéhr
	email = leo@bergnehr.se

[alias]
  l = log --graph --decorate --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  st = status
  ci = commit
  co = checkout
  di = diff --color
  dc = diff --cached --color
  amend = commit --amend
  aa = add --all
  ff = merge --ff-only
  pullff = pull --ff-only
  noff = merge --no-ff
  fa = fetch --all
  pom = push origin master
  poh = push origin head
  b = branch
  ds = diff --stat=161,120 --color
  dh1 = diff HEAD~1 --color

[diff]
  tool = vimdiff

[difftool]
  promt = false
